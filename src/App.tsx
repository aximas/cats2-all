import React from 'react';
import {MainRoutes} from '@routes/MainRoutes';

function App() {

    return (
        <div className={'container'}>
            <MainRoutes />
        </div>
    );
}

export default App;
